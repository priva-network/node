sudo apt update && sudo apt install git
ssh-keygen -t rsa -b 4096 -C "m13basra@gmail.com"
cat ~/.ssh/id_rsa.pub
-- ADD TO GITHUB --
ssh -T git@github.com
git clone git@github.com:manveerxyz/cortex-node.git

sudo apt update
sudo apt install python3.9
python3.9 -m pip install poetry

sudo apt install nvidia-cuda-toolkit
nvcc --version

python3.9 -m poetry shell
python3.9 -m pip install poetry
poetry install
python3.9 -m pip install torch==1.8.1+cu101 torchvision==0.9.1+cu101 torchaudio==0.8.1 -f https://download.pytorch.org/whl/torch_stable.html